import React from "react";

const NotFoundPage = () => (
  <div>
    <h1>NOT FOUND</h1>
    <p>Do not go gently into that good night... press the home button above to return to the fold. Or, operate the menu </p>
  </div>
);

export default NotFoundPage;
